import { Bars } from "react-loader-spinner";

const Loader = () => {
  return (
    <div>
      <Bars
        height="20"
        width="20"
        color="#fff"
      />
    </div>
  );
};

export default Loader;
